---
/**
 * Modulo 2 — Marcacion de zonas.
 *
 * El modulo mas largo del recorrido, y con razon: casi todo el plan se apoya
 * en que una zona este bien dibujada.
 *
 * Reestructurado el 03/09/2026 a peticion del operador: ocho apartados, uno
 * por casuistica, y cada uno con su imagen generada sobre mercado real. El
 * vocabulario tambien es suyo: se dice MOVIMIENTO (no tramo) y despues del
 * rompimiento se dice CONSECUCION (no confirmacion), que es el termino del
 * glosario.
 *
 * Solo reglas confirmadas de 01_Plan/reglas.json. Nada del PDF de parametros
 * que no este ya en el plan.
 */
import Modulo from '../componentes/Modulo.astro';
import * as P from '../lib/parsers.mjs';

const par = P.parametros();
const v = (n, alt) => par.get(n)?.valor ?? alt;

const SECCIONES = [
  { id: 'movimiento', t: 'Corrida y retroceso' },
  { id: 'nace', t: 'De dónde sale' },
  { id: 'consecucion', t: 'Rompimiento y consecución' },
  { id: 'estira', t: 'Sin consecución' },
  { id: 'apendice', t: 'La zona apéndice' },
  { id: 'convivencia', t: 'Varias zonas a la vez' },
  { id: 'caduca', t: 'Cuándo deja de contar' },
  { id: 'flujo', t: 'El recorrido completo' },
];
---

<Modulo slug="zonas" secciones={SECCIONES}>
  <!-- 1 ─────────────────────────────────────────────────────────────── -->
  <section class="ap" id="movimiento">
    <h2>Corrida y retroceso <span class="solo-tecnico mono ref-cod">R-10 · R-11</span></h2>
    <p>
      El mercado solo hace dos cosas: avanza un movimiento y después se para a
      descansar. Al movimiento que avanza se le llama <strong>corrida</strong>.
      Al descanso, <strong>retroceso</strong>. No hay un tercer estado, y esta
      pareja es la que deja todas las zonas del día.
    </p>

    <figure class="gr">
      <img src="/conceptos/02-corrida-retroceso.png" alt="Un movimiento alcista real separado en su corrida y su retroceso" loading="eager" />
      <figcaption class="meta">Movimiento alcista: la corrida sube y el retroceso baja. La resistencia sale de la vela más alta de la corrida.</figcaption>
    </figure>

    <figure class="gr">
      <img src="/conceptos/16-corrida-bajista.png" alt="El mismo mecanismo en un movimiento bajista real" loading="eager" />
      <figcaption class="meta">El mismo mecanismo hacia abajo: la corrida baja y el retroceso sube. El soporte sale de la vela más baja.</figcaption>
    </figure>

    <dl class="datos">
      <dt>Cuándo nace una corrida</dt>
      <dd>Cuando una vela supera el extremo de la anterior</dd>
      <dt>Mientras vive</dt>
      <dd>Cada vela respeta el extremo contrario de la anterior. Si empatan, sigue viva</dd>
      <dt>Cuándo muere</dt>
      <dd>En la primera vela que va en contra, aunque sea <span class="cifra-en">1</span> tick</dd>
      <dt>Cuántas velas dura</dt>
      <dd>Mínimo dos. Máximo, ninguno</dd>
      <dt>El color de la vela</dt>
      <dd>No importa en absoluto</dd>
      <dt>Una vela dentro de la anterior</dt>
      <dd>No corta la corrida</dd>
    </dl>

    <p>
      La vela que mata la corrida <strong>ya es la primera del retroceso</strong>.
      No hay hueco entre una cosa y la otra: el movimiento cambia de fase en esa
      misma vela.
    </p>

    <div class="duro">
      <p>
        <strong>Un retroceso no tiene tamaño mínimo ni máximo de velas.</strong>
        Puede ser una sola vela. No se descarta un movimiento por parecer
        demasiado corto o demasiado largo.
      </p>
    </div>

    <p>
      Del retroceso interesa un punto concreto: <strong>su extremo</strong> — el
      más bajo si la corrida era alcista, el más alto si era bajista. Ahí irá el
      stop cuando llegue el momento, y ese punto reaparece en casi todas las
      decisiones que vienen después.
    </p>
  </section>

  <!-- 2 ─────────────────────────────────────────────────────────────── -->
  <section class="ap" id="nace">
    <h2>De dónde sale la zona <span class="solo-tecnico mono ref-cod">R-12 · R-33 · R-38</span></h2>
    <p>
      Cuando una corrida termina, deja un rastro en el gráfico: una franja de
      precio que se llama zona. No se dibuja a ojo. Sale siempre de la misma
      vela y con los mismos límites.
    </p>

    <figure class="gr">
      <img src="/conceptos/17-vela-designada.png" alt="La vela designada de un movimiento y los dos bordes exactos de la zona" loading="eager" />
      <figcaption class="meta">La vela designada y sus dos bordes: del borde del cuerpo a la punta de la mecha. Ni un tick más, ni un tick menos.</figcaption>
    </figure>

    <ol class="secuencia">
      <li><strong>Se busca la vela designada</strong>: la del máximo más alto si la corrida fue alcista, la del mínimo más bajo si fue bajista. Se busca desde el origen de la corrida hasta la vela que disparó el retroceso, <strong>esa incluida</strong>.</li>
      <li><strong>Se dibuja del borde del cuerpo a la punta de la mecha</strong> de esa vela. Ese es todo el criterio.</li>
      <li><strong>Se extiende hacia la derecha</strong>, a lo largo del gráfico.</li>
    </ol>

    <figure class="gr">
      <img src="/conceptos/03-zona.png" alt="La zona ya dibujada y extendida hacia la derecha sobre una sesión real" loading="lazy" />
      <figcaption class="meta">Ya extendida: el rectángulo se dibuja desde la vela de origen, no desde donde el precio vuelva más tarde.</figcaption>
    </figure>

    <dl class="datos">
      <dt>Corrida alcista</dt>
      <dd>Deja <strong>resistencia</strong> arriba</dd>
      <dt>Corrida bajista</dt>
      <dd>Deja <strong>soporte</strong> abajo</dd>
      <dt>Y su retroceso</dt>
      <dd>El retroceso de una corrida alcista deja <strong>soporte</strong>; el de una bajista, resistencia</dd>
      <dt>Vela sin mecha</dt>
      <dd>La zona es una línea en el extremo de la vela</dd>
      <dt>Vela sin cuerpo</dt>
      <dd>Apertura y cierre coinciden: de ese precio a la punta de la mecha</dd>
      <dt>Una vela con máximo mayor y mínimo menor</dt>
      <dd>El orden dentro de la vela decide cuál sostiene la zona</dd>
    </dl>

    <h3>Cuándo se dibuja cada una</h3>
    <p>
      Las dos zonas de un movimiento no nacen a la vez, y confundirlo lleva a
      operar algo que todavía no existe:
    </p>
    <div class="rejilla dos">
      <div class="tarj">
        <h3>La zona de la corrida</h3>
        <p>Se dibuja <strong>en cuanto aparece el retroceso</strong>, en vivo. Ya se puede operar.</p>
      </div>
      <div class="tarj">
        <h3>La zona del retroceso</h3>
        <p>Mientras el retroceso vive es solo una <strong>línea provisional</strong> que se mueve con cada vela. Se convierte en zona cuando aparece el movimiento contrario.</p>
      </div>
    </div>

    <div class="veto">
      <p>
        <strong>Una línea provisional no opera.</strong> No admite rompimiento,
        ni consecución, ni reingreso. Hasta que no es zona, no existe para el
        método.
      </p>
    </div>
  </section>

  <!-- 3 ─────────────────────────────────────────────────────────────── -->
  <section class="ap" id="consecucion">
    <h2>Rompimiento y consecución <span class="solo-tecnico mono ref-cod">R-13 · R-37 · R-38</span></h2>
    <p>
      Superar una zona son dos cosas, no una. Primero el rompimiento, después la
      consecución. Y el mismo mecanismo que supera una zona es el que más tarde
      dispara la entrada.
    </p>

    <figure class="gr">
      <img src="/conceptos/14-traspaso.png" alt="La vela que pasa el borde de la zona y la que después pasa de su extremo" loading="eager" />
      <figcaption class="meta">Las dos velas del traspaso sobre una sesión real: la que rompe, y la consecución que pasa de su extremo.</figcaption>
    </figure>

    <div class="rejilla dos">
      <div class="tarj">
        <span class="tarj-n">1</span>
        <h3>Rompimiento</h3>
        <p>El precio pasa el borde de la zona por al menos <strong>un tick</strong>.</p>
        <p>Basta la mecha. <strong>El cierre da igual.</strong></p>
      </div>
      <div class="tarj">
        <span class="tarj-n">2</span>
        <h3>Consecución</h3>
        <p>Una vela posterior pasa por un tick el <strong>extremo de la vela que rompió</strong>.</p>
        <p>Recién ahí la zona quedó traspasada.</p>
      </div>
    </div>

    <h3>Dos maneras de romper</h3>
    <p>
      El cierre no decide si hay rompimiento, pero sí deja un rastro distinto.
      Esa diferencia no se nota hoy: se nota en los dos apartados siguientes, si
      la consecución no llega.
    </p>

    <figure class="gr">
      <img src="/conceptos/18-rompe-mecha.png" alt="Una vela que pasa el borde con la mecha y cierra dentro de la zona" loading="lazy" />
      <figcaption class="meta">Rompimiento con mecha: la vela pasa el borde pero cierra dentro de la zona. Sigue siendo rompimiento.</figcaption>
    </figure>

    <figure class="gr">
      <img src="/conceptos/19-rompe-cuerpo.png" alt="Una vela que pasa el borde y cierra fuera de la zona" loading="lazy" />
      <figcaption class="meta">Rompimiento con cuerpo: la vela cierra al otro lado del borde.</figcaption>
    </figure>

    <div class="duro">
      <p>
        <strong>Un rompimiento solo, sin consecución, no invalida nada.</strong>
        La zona sigue viva y sigue contando para todo.
      </p>
      <p>
        Y la vela que da la consecución de un traspaso no abre a la vez el
        rompimiento del lado contrario: eso se busca a partir de la siguiente.
      </p>
    </div>

    <p>
      La consecución puede tardar. Para traspasar una zona
      <strong>no hay plazo</strong>: el rompimiento queda pendiente
      indefinidamente y la consecución lo cierra cuando llegue. El plazo de
      {v('PLAZO_CONSECUCION', '5 velas')} que viene a continuación gobierna
      otra cosa: la forma de la zona y la vida de la orden.
    </p>
  </section>

  <!-- 4 ─────────────────────────────────────────────────────────────── -->
  <section class="ap" id="estira">
    <h2>Cuando no llega la consecución <span class="solo-tecnico mono ref-cod">R-15 · R-19</span></h2>
    <p>
      Si el precio rompe una zona y pasan {v('PLAZO_CONSECUCION', '5 velas')} sin
      que llegue la consecución, la zona cambia de forma. Qué le pasa depende de
      cómo rompió aquella vela.
    </p>

    <figure class="gr">
      <img src="/conceptos/05-sin-confirmar.png" alt="Una vela cruza la zona y ninguna de las siguientes supera su extremo" loading="eager" />
      <figcaption class="meta">El caso más común: la vela cruza la zona, pero ninguna de las siguientes pasa de su extremo. No hubo traspaso y la zona sigue viva.</figcaption>
    </figure>

    <p>
      Cuando el rompimiento fue <strong>con la mecha</strong> —el cierre se quedó
      dentro— la zona <strong>se estira</strong> hasta la punta de esa mecha. El
      otro borde no se mueve. Sigue habiendo una sola zona, más grande, y
      conserva su historial.
    </p>

    <dl class="datos">
      <dt>Qué lo dispara</dt>
      <dd>Rompimiento con mecha <strong>+ {v('PLAZO_CONSECUCION', '5 velas')}</strong> sin consecución</dd>
      <dt>Nuevo límite</dt>
      <dd>La punta de la mecha de la vela que rompió</dd>
      <dt>El borde opuesto</dt>
      <dd>No se mueve</dd>
      <dt>Zonas resultantes</dt>
      <dd><span class="cifra-en">1</span>, más grande</dd>
    </dl>

    <div class="duro">
      <p>
        <strong>Las {v('PLAZO_CONSECUCION', '5 velas')} son un tope, no una espera obligatoria.</strong>
        Si antes de que se cumplan aparece un retroceso nuevo, se marca zona en
        ese momento y no se espera más.
      </p>
      <p>Actúa lo que llegue primero.</p>
    </div>

    <p>
      Con una salvedad: el único retroceso que <em>no</em> cuenta como estructura
      nueva es el que quedó bloqueado por la regla de la mitad entre zonas. Si es
      ese, el reloj de las {v('PLAZO_CONSECUCION', '5 velas')} sigue corriendo.
    </p>
  </section>

  <!-- 5 ─────────────────────────────────────────────────────────────── -->
  <section class="ap" id="apendice">
    <h2>La zona apéndice <span class="solo-tecnico mono ref-cod">R-16</span></h2>
    <p>
      Es el otro desenlace del mismo plazo. Si la vela que rompió lo hizo
      <strong>con el cuerpo</strong> —cerró fuera de la zona— y pasan
      {v('PLAZO_CONSECUCION', '5 velas')} sin consecución, no se estira nada:
      <strong>nace una zona nueva</strong>, pegada a la anterior. Se llama zona
      apéndice.
    </p>

    <figure class="gr">
      <img src="/conceptos/20-apendice-nace.png" alt="Rompimiento con cuerpo sin consecución: la zona original y la apéndice que nace bajo ella" loading="eager" />
      <figcaption class="meta">La original se queda intacta y la apéndice se marca sobre la mecha de la vela que rompió. Ahora hay dos.</figcaption>
    </figure>

    <dl class="datos">
      <dt>Qué lo dispara</dt>
      <dd>Rompimiento con cuerpo <strong>+ {v('PLAZO_CONSECUCION', '5 velas')}</strong> sin consecución</dd>
      <dt>La zona original</dt>
      <dd>No se modifica</dd>
      <dt>Un borde de la apéndice</dt>
      <dd>El borde del cuerpo de la vela que rompió</dd>
      <dt>El otro borde</dt>
      <dd>El extremo de su mecha</dd>
      <dt>Zonas resultantes</dt>
      <dd><span class="cifra-en">2</span>: la original y su apéndice</dd>
    </dl>

    <figure class="gr">
      <img src="/conceptos/10-zona-apendice.png" alt="Otro caso real de zona apéndice sobre la mecha de la vela de rompimiento" loading="lazy" />
      <figcaption class="meta">Otro caso real: la apéndice ocupa exactamente la mecha que sobresalió, nada más.</figcaption>
    </figure>

    <div class="duro">
      <p>
        <strong>La apéndice no nace por acción del precio sobre ella.</strong>
        No es una zona más que apareció: es el rastro de un rompimiento que se
        quedó sin consecución.
      </p>
    </div>

    <div class="veto">
      <p>
        <strong>Cómo se entra sobre una zona apéndice está pendiente.</strong>
        El plan la marca y la mantiene, pero las reglas de entrada asociadas
        todavía no están cerradas. Hasta que lo estén, no se deduce nada.
      </p>
    </div>
  </section>

  <!-- 6 ─────────────────────────────────────────────────────────────── -->
  <section class="ap" id="convivencia">
    <h2>Varias zonas a la vez <span class="solo-tecnico mono ref-cod">R-17 · R-18 · R-35 · R-36</span></h2>
    <p>
      En una jornada aparecen muchas zonas candidatas. Tres reglas evitan que el
      gráfico se llene de rectángulos y que se acabe operando ruido.
    </p>

    <h3>Si se tocan, se funden</h3>
    <p>
      Si la zona que se iba a marcar toca una que ya existe —basta que se rocen
      los bordes— <strong>no se crea una nueva</strong>: se estira la que ya
      estaba hasta el extremo nuevo. El otro borde no se mueve, no se engloba
      nada, y la zona conserva su historial de rompimientos y consecuciones.
    </p>

    <figure class="gr">
      <img src="/conceptos/21-zonas-se-funden.png" alt="Dos zonas que se tocan y quedan convertidas en una sola, estirada" loading="eager" />
      <figcaption class="meta">No quedan dos zonas pegadas: queda una sola, estirada hasta el extremo nuevo.</figcaption>
    </figure>

    <p>
      Si la candidata cae entera dentro de la existente, no hay nada que hacer:
      no se toca nada. Y dos zonas de tipo distinto no se solapan mientras una
      esté viva: una zona viva ocupa su franja de precio.
    </p>

    <h3>Entre dos zonas, solo si no cruza la mitad</h3>
    <p>
      Cuando hay una zona por arriba y otra por abajo, se calcula el punto medio
      entre sus bordes internos. Solo se marca zona nueva si el movimiento que la
      genera <strong>se queda entero de un lado</strong> de esa mitad.
    </p>

    <figure class="gr">
      <img src="/conceptos/22-entre-zonas.png" alt="Una zona marcada entre otras dos, con la mitad de la banda señalada" loading="lazy" />
      <figcaption class="meta">El movimiento entero se queda del lado en el que empezó. Por eso esta zona sí se marca.</figcaption>
    </figure>

    <div class="duro">
      <p>
        <strong>Lo que se mide es el recorrido del precio, no el rectángulo.</strong>
        Si el movimiento cruzó la mitad en algún punto, no se marca — aunque la
        caja de la zona quede entera a un lado.
      </p>
    </div>

    <p>
      La mitad se recalcula cada vez, contra la zona más cercana por arriba y la
      más cercana por abajo <em>en ese momento</em>. No hay límite de zonas
      intermedias en una jornada. Pasa poco, pero pasa.
    </p>

    <h3>Una sola por franja y por jornada</h3>
    <p>
      La franja entre dos zonas admite <strong>una zona en todo el día</strong>,
      y el turno lo resuelve el primer retroceso que aparezca dentro. Si ese
      primero respeta la mitad, se marca; si no la respeta, no se marca. En los
      dos casos <strong>la franja queda cerrada</strong> para el resto de la
      jornada, y no se reabre aunque las zonas que la formaban se mueran.
    </p>
    <p>
      Cualquier zona viva sirve de borde de la franja, incluidas las que se
      marcaron de madrugada por volumen.
    </p>

    <div class="veto">
      <p>
        <strong>Salir de una zona es rompimiento y consecución, no geometría.</strong>
        Mientras un rompimiento espera su consecución, no se marca zona al otro
        lado de esa zona.
      </p>
    </div>
  </section>

  <!-- 7 ─────────────────────────────────────────────────────────────── -->
  <section class="ap" id="caduca">
    <h2>Cuándo deja de contar <span class="solo-tecnico mono ref-cod">R-14</span></h2>
    <p>
      Una zona muere cuando ha sido superada <strong>en las dos direcciones</strong>:
      rompimiento y consecución hacia un lado, y rompimiento y consecución hacia
      el otro.
    </p>

    <figure class="gr">
      <img src="/conceptos/23-zona-invalida.png" alt="Una zona traspasada primero hacia un lado y después hacia el otro" loading="eager" />
      <figcaption class="meta">Traspasada en los dos sentidos. A partir de ahí no bloquea ni sirve para entrar.</figcaption>
    </figure>

    <dl class="datos">
      <dt>Solo rompimiento, sin consecución</dt>
      <dd>La zona sigue vigente. No ha pasado nada</dd>
      <dt>Superada en un solo sentido</dt>
      <dd>Sigue viva. Cambia de papel: el soporte pasa a resistencia y al revés</dd>
      <dt>Superada en los dos</dt>
      <dd><strong>No cuenta para nada.</strong> Ni bloquea un objetivo, ni sirve para entrar, ni cuenta para medir la mitad entre zonas</dd>
      <dt>En pantalla</dt>
      <dd>Se deja dibujada en tono muy tenue, solo como recuerdo</dd>
    </dl>

    <p>
      Esa distinción importa más de lo que parece: una zona que sigue viva puede
      impedir una entrada perfectamente válida, porque se cruza en el camino del
      objetivo.
    </p>
  </section>

  <!-- 8 ─────────────────────────────────────────────────────────────── -->
  <section class="ap" id="flujo">
    <h2>El recorrido completo</h2>
    <p>
      Todo lo anterior, en el orden en que ocurre en el gráfico. Cada paso es una
      de las reglas de este módulo.
    </p>

    <div class="flujo">
      <div class="fnodo">
        <span class="fn">1</span>
        <p>Una <strong>corrida</strong> avanza y muere en la primera vela que va en contra. Esa vela ya es el <strong>retroceso</strong>.</p>
      </div>

      <div class="fnodo">
        <span class="fn">2</span>
        <p>Al aparecer el retroceso se marca la <strong>zona</strong> sobre la vela designada, del borde del cuerpo a la punta de la mecha, y se extiende hacia la derecha.</p>
      </div>

      <div class="fnodo">
        <span class="fn">3</span>
        <p>¿Toca una zona que ya existe? Entonces <strong>no nace una nueva</strong>: se estira la que estaba. ¿Está entre dos zonas y el movimiento cruzó la mitad? <strong>No se marca</strong>, y la franja queda cerrada por hoy.</p>
      </div>

      <div class="fnodo">
        <span class="fn">4</span>
        <p>Más adelante el precio vuelve y pasa el borde por <strong>un tick</strong>: hay <strong>rompimiento</strong>. Basta la mecha.</p>
      </div>

      <div class="fnodo fpregunta">
        <span class="fn">5</span>
        <p>¿Llega la <strong>consecución</strong> — una vela que pasa del extremo de la que rompió?</p>
      </div>

      <div class="fbifurca">
        <div class="fnodo fsi">
          <span class="fetiq">Sí</span>
          <p>La zona queda <strong>traspasada</strong> en ese sentido y cambia de papel. Ese mismo momento es el que dispara la entrada.</p>
        </div>
        <div class="fnodo fno">
          <span class="fetiq">No, y pasan {v('PLAZO_CONSECUCION', '5 velas')}</span>
          <p>Si rompió <strong>con la mecha</strong>, la zona <strong>se estira</strong>. Si rompió <strong>con el cuerpo</strong>, nace la <strong>apéndice</strong>.</p>
        </div>
      </div>

      <div class="fnodo">
        <span class="fn">6</span>
        <p>Cuando la zona ha sido traspasada <strong>en los dos sentidos</strong>, deja de contar. Ni bloquea un objetivo, ni sirve para entrar.</p>
      </div>
    </div>
  </section>
</Modulo>

<style>
  .ref-cod { margin-left: var(--e-2); font-size: var(--t-1); color: var(--texto-3); }

  /* Diagrama del recorrido: los pasos del modulo encadenados, con el hilo
     vertical que los une. Es el resumen del apartado 8; no dibuja mercado,
     asi que aqui no aplica el estandar de graficas. */
  .flujo { display: grid; gap: var(--e-3); margin: var(--e-6) 0; }

  .fnodo {
    position: relative;
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    gap: var(--e-3);
    align-items: start;
    padding: var(--e-4);
    border-radius: var(--radio-3);
    background: var(--vidrio-fondo);
    border: 1px solid var(--vidrio-borde);
  }
  .fnodo > p { margin: 0; color: var(--texto-2); font-size: var(--t-3); }
  .fnodo > p strong { color: var(--texto); font-weight: var(--peso-medio); }

  /* El hilo que baja de un nodo al siguiente. */
  .flujo > .fnodo:not(:last-child)::after,
  .flujo > .fbifurca::after {
    content: '';
    position: absolute;
    left: 50%;
    bottom: calc(var(--e-3) * -1);
    width: 2px;
    height: var(--e-3);
    /* El borde de vidrio es demasiado tenue para un hilo de 2px sobre el
       fondo; el hilo tiene que verse o el diagrama no encadena. */
    background: color-mix(in srgb, var(--texto-3) 55%, transparent);
  }

  .fn {
    display: grid;
    place-items: center;
    width: 1.75rem;
    height: 1.75rem;
    border-radius: 50%;
    background: var(--n-800);
    color: var(--texto-3);
    font-family: var(--fuente-mono);
    font-size: var(--t-1);
    font-variant-numeric: tabular-nums;
  }

  .fpregunta { border-color: color-mix(in srgb, var(--acento) 35%, transparent); }
  .fpregunta .fn { background: color-mix(in srgb, var(--acento) 22%, var(--n-800)); color: var(--texto); }

  .fbifurca {
    position: relative;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(15rem, 1fr));
    gap: var(--e-3);
  }
  .fbifurca .fnodo { grid-template-columns: minmax(0, 1fr); gap: var(--e-2); }
  .fbifurca .fnodo::after { content: none; }

  .fetiq {
    font-size: var(--t-1);
    text-transform: uppercase;
    letter-spacing: var(--track-etiqueta);
    font-weight: var(--peso-fuerte);
  }
  .fsi { border-color: color-mix(in srgb, var(--positivo) 30%, transparent); }
  .fsi .fetiq { color: var(--positivo); }
  .fno { border-color: color-mix(in srgb, var(--aviso) 30%, transparent); }
  .fno .fetiq { color: var(--aviso); }
</style>
