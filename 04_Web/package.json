{
  "name": "chaumer-portal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Portal de revision del plan de trading Chaumer. Renderiza 01_Plan/ - no anade metodologia.",
  "scripts": {
    "sync": "node scripts/sync.mjs",
    "dev": "npm run sync && npm run diagramas && astro dev",
    "build": "npm run sync && npm run diagramas && astro build",
    "preview": "astro preview",
    "fuentes": "node scripts/contar-fuentes.mjs",
    "diagramas": "node scripts/diagramas.mjs"
  },
  "devDependencies": {
    "@mermaid-js/mermaid-cli": "^11.16.0",
    "astro": "^7.2.10",
    "wrangler": "^4.127.1"
  },
  "dependencies": {
    "@phosphor-icons/core": "^2.1.1",
    "geist": "^1.7.2",
    "marked": "^18.0.11"
  }
}
